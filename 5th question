#include<stdio.h>
int main(){
char str[100];
int length=0;
printf("Enter the string:");
fgets(str,sizeof(str),stdin);
char *ptr=str;
while(*ptr!='\0'){
length ++;
ptr ++;
}
printf("%d",length);
return 0;
}
